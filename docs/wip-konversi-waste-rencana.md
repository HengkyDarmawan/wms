# RANCANGAN WIP — modul Konversi & Waste (belum selesai, belum diuji)

> Disalin dari berkas rencana agen di sesi kantor, 24 Sep 2026. Baca bersama [prompts/00-lanjutkan-di-rumah.md](prompts/00-lanjutkan-di-rumah.md). Hapus berkas ini setelah modul selesai.

# Modul Conversion/Waste (CNV + WST) Fase 1 — status & sisa langkah

Dihentikan karena sesi masuk plan mode (24 Sep 2026). Tidak ada commit. `php artisan test` BELUM dijalankan sejak mulai (baseline 493 hijau).

## Sudah dibuat (file baru, belum diuji)
- `database/migrations/tenant/2026_01_01_000130_create_conversion_waste_tables.php` — tabel `conversion_recipes` (stub F2), `conversions`, `conversion_inputs`, `conversion_outputs`, `waste_disposals`, `waste_disposal_lines` (kolom lengkap, lihat file).
- `app/Domain/Conversion/Enums/ConversionStatus.php` (draft, pending_approval, completed, cancelled — KS §2.10)
- `app/Domain/Conversion/Enums/ConversionType.php` (cut, assemble, disassemble, repack; `requiresSizeBalance()` = cut|repack)
- `app/Domain/Conversion/Enums/ConversionOutputKind.php` (output, offcut, waste, kerf; `movesStock()`, `isRemainder()`, `stockStatus()`)
- `app/Domain/Conversion/Exceptions/ConversionRuleException.php`
- `app/Domain/Conversion/Models/{Conversion,ConversionInput,ConversionOutput,ConversionRecipe}.php`
- `app/Domain/Conversion/Support/ConvertibleStock.php` (calon input: bin storage, Tersedia, − alokasi keras; key `bin:item:lot:piece`; potongan utuh; aset/serial ditolak)
- Direktori kosong: `app/Domain/{Conversion,Waste}/...`, `app/Http/Controllers/{Conversion,Waste}`, `resources/views/{conversion,waste,livewire/conversion,livewire/waste}`, `tests/Feature/Conversion/Concerns`

## Keputusan desain (jadi asumsi A-153…A-161, *Perlu validasi*, `04` §2.9 baru)
- A-153 CNV tanpa status baru: `conversion.submit` hanya bila ada aturan approval cocok (engine `matchRule` + konteks handler) → `pending_approval` lalu `ApprovalEngine::submit`; `conversion.complete` hanya bila TIDAK ada aturan (draft→completed langsung); `conversion.approve` lewat mesin → handler `onApproved` posting → completed; ditolak → kembali `draft` + `reject_reason_id` (ajukan ulang/batal); fallbackSteps = [].
- A-154 Input/baris CNV: input dari bin penyimpanan Tersedia (A-85), habis pakai, bukan serial; potongan dipakai utuh; draf tidak memegang stok (cek saat simpan & ulang saat submit/complete); gudang Site → proyek harus pemilik site; output boleh item lain (habis pakai, bukan serial); item per potong: satu baris = satu potongan (`count` menggandakan, maks 100); output berlot: warisi lot input item sama atau `lot_no` wajib (lot dibuat saat selesai); waste → bin Waste kondisi Rusak (seperti A-113); kerf tanpa pergerakan.
- A-155 Kolom di luar ERD: conversions.number,status,reversal_of_id,reason_code_id,prepared_by,submitted_by/at,approved_by/at,reject_reason_id,completed_by,cancel_reason_id,cancelled_at,notes; conversion_inputs.reversal_of_line_id,movement_id; conversion_outputs.stock_status,lot_no,lot_id,reason_code_id,auto_waste,reversal_of_line_id,movement_id; waste_disposals.number,status,target_bin_id,evidence_path,evidence_note,submitted_by,approved_by/at,reject_reason_id,closed_by,cancel_reason_id,cancelled_at,notes; waste_disposal_lines.item_id,lot_id,serial_id,piece_id,stock_status,movement_id.
- A-156 Neraca: cut/repack satu satuan dasar, |Σin − Σ(output+offcut+waste+kerf)| ≤ max(0.00005, uom.rounding/2); assemble/disassemble tanpa neraca (rasio menunggu resep F2); minimal 1 output/offcut; offcut hanya item per potong & item sama input induk; offcut < min_offcut_length → otomatis waste (`auto_waste`); sisa selalu item input induk; parent kosong = input pertama.
- A-157 CNV pembalik: CNV baru `reversal_of_id`, `draft`, Alasan `*` (konteks cancel), salin baris (`reversal_of_line_id`); mesin status sama (submit/complete); posting `StockLedger::reverse()` hasil dulu lalu input, kejadian `material_converted` + `reverses_event_id`; potongan baru `is_consumed=true`, potongan input `is_consumed=false`; BR-CNV-05 = semua output/offcut/waste masih di bin (saldo − alokasi keras ≥ jumlah per kombinasi) dan potongan tanpa pergerakan setelah CNV; dicek saat buat & saat posting; satu pembalik aktif per CNV.
- A-158 Permission: conversion.view/create/submit/complete/approve/cancel (6), waste.view/create/approve/close/cancel (5). Admin & Kepala Gudang semua; Staf Gudang CNV view/create/submit/complete/cancel, WST view/create/close/cancel; Manajemen view+approve keduanya; Auditor Int/Eks view keduanya; Pemohon/Driver/PR/Klien tidak. Batal oleh pembuat/pengaju atau pemegang approve; ubah draf CNV oleh pembuat atau pemegang complete. Cakupan gudang+proyek (ScopedToUser).
- A-159 WST: proyek wajib (BR §1; Site → proyek pemilik); baris dari saldo bin Waste gudang (semua kondisi), kunci `bin:item:lot:serial:piece:status`; jumlah ≤ saldo − WST terbuka lain (submitted/pending/approved); potongan utuh, serial 1; disposisi header; `reused` → `target_bin_id` bin penyimpanan aktif, kondisi Tersedia (fromStockStatus asal); create → submitted → pending_approval + engine submit (tanpa aturan = approved otomatis, A-08); ditolak → `rejected`; close hanya dari approved, cek ulang saldo; alasan baris opsional konteks waste.
- A-160 Bukti tutup WST: foto (StoreUpload gambar, `waste-disposals/{id}`) ATAU `evidence_note` (nomor BA bertanda tangan) — salah satu wajib; `evidence_attachment_id` tanpa FK; route GET bukti berotorisasi. Cetak: `waste_disposal` stub diaktifkan (BA Waste, blok Dibuat oleh · Disetujui · Saksi), jenis baru `conversion` = Bukti Konversi Material (blok Dikerjakan oleh · Disetujui · PIC proyek); `isStub()` tinggal AssetHandover.
- A-161 Laporan Material per proyek kolom baru: Dikonversi (input CNV keluar − pembalik), Hasil konversi (output+offcut masuk − pembalik), Waste (CNV waste + RET pilah waste ke bin Waste ber-proyek, − pembalik), Waste didisposisi (WST closed, per proyek WST).

## Sisa langkah (urut)
1. `app/Domain/Conversion/Support/ConversionLines.php` — normalisasi baris hasil + `assertBalanced(Conversion)` + `totals()` + `binWaste()` (draf isi sudah dirancang; tulis ulang sesuai A-154/A-156).
2. `Master\Models\Piece::nextPieceNo()` (static, loop `P-%06d`) dan pakai di `Return\Actions\SortGoodsReturn::potonganBaru` (selaraskan).
3. `Conversion\Support\ConversionPoster` (post: input keluar + piece consumed; output/offcut/waste masuk + potongan baru `origin_type=conversion`, `parent_piece_id`, `is_offcut` utk offcut; lot firstOrCreate; kejadian `material_converted` payload role/number/type/parent/kerf_total; reverse), `ConversionReversibility` (BR-CNV-05), `ConversionApprovalHandler` (context warehouse/project/kategori/kepemilikan/lineCount/maxLineQty, SoD prepared_by+submitted_by, onApproved→post+completed, onRejected→draft).
4. Actions: `CreateConversion` (handle/update/reverse), `SubmitConversion`, `CompleteConversion`, `ApproveConversion` (approve/reject via DecideApproval), `CancelConversion` (withdraw snapshot bila pending). Policy `ConversionPolicy` (viewAny/view/create/update/submit/complete/cancel/approve/reverse).
5. Livewire `ConversionList/Form/Detail` + concern `HandlesConversionRules`; views `livewire/conversion/*`, `conversion/{index,create,edit,show}`; controller `Conversion\ConversionController`; `ConversionServiceProvider` (policy, registry `ApprovalDocumentType::Conversion`, Livewire `conversion.*`).
6. Domain Waste: enums `WasteDisposalStatus` (submitted,pending_approval,approved,rejected,closed,cancelled), `WasteDisposition`; exception; models `WasteDisposal`, `WasteDisposalLine`; `Support\DisposableWaste`, `WastePoster`, `WasteDisposalApprovalHandler`; actions `CreateWasteDisposal`, `ApproveWasteDisposal`, `CloseWasteDisposal`, `CancelWasteDisposal`; policy; Livewire list/form/detail; controller (+ route bukti); `WasteServiceProvider`.
7. Routes GET `/conversions`, `/conversions/create`, `/conversions/{conversion}`, `/conversions/{conversion}/edit`, `/waste-disposals`, `/waste-disposals/create`, `/waste-disposals/{wasteDisposal}`, `/waste-disposals/{wasteDisposal}/evidence`; `bootstrap/providers.php`; sidebar seksi "Konversi & waste"; palet Ctrl+K (Konversi material, Konversi baru, Berita acara waste, BA waste baru).
8. Template: `DocumentTemplateType::Conversion` ('conversion', label Bukti Konversi Material), WasteDisposal aktif, `defaultSignatureBlocks`, `DocumentPrinter` find/view/url + views `print/documents/{conversion,waste_disposal}.blade.php`; tombol cetak di detail.
9. `ReferenceSeeder`: modul `conversion` (6) & `waste` (5) + role mapping (A-158); `HandlesApprovalRules` tangkap `ConversionRuleException|WasteRuleException`.
10. `ProjectMaterialSummary` + `ProjectMaterialReport` kolom baru (A-161).
11. Uji `tests/Feature/Conversion/*` (fixture: pipa cuttable M, min_offcut 1.0, kerf 0.005; 6 m batang → 2×2.5 m output + 0.99 m? catatan: 0.99 < 1.0 berarti offcut otomatis waste — pakai min 0.5 untuk rantai offcut 0.99 + kerf 0.01, dan kasus terpisah offcut 0.3 < min → waste → WST). TC-CNV-01…~14, TC-WST-01…~8, screen tests 200/403/404, chain & report. Ubah uji lama: `RuleSimulationTest` TC-APR-17 (`waste_disposal` → `purchase_request`), `DocumentPrintTest` TC-TPL-04 (hapus 501 waste-disposal), `DemoSeederTest` TC-ACC-27b (+conversion 6, waste 5, except list).
12. `py -3 docs/diagram/_generate_erd.py` setelah menambah kolom (A-155) + versi model 0.12→0.13; `php artisan tenants:migrate`; DemoSeeder; `php artisan test`; `py -3 docs/diagram/_verify.py`; `npm run build`.
13. Dokumen: `docs/wms/24-konversi-waste.md` v0.2 (template, ≤450 baris, TC-CNV/TC-WST, §13); `06` v0.14 (enum `conversion_type`, `conversion_output_kind`, `document_template_type` + `conversion`, WST aktif, catatan §2.10/§2.14, tanpa status baru); `04` v0.20 §2.9 A-153–A-161 + matriks; README v0.30 changelog + baris folder; `00-laporan-progres` baris "Konversi material, offcut, waste" ✅ + ringkasan; `cek.md`; §13 di 23-pemakaian, 22-retur-transfer (Piece::nextPieceNo), 18-template, 20-approval (TC-APR-17), 16-shared (kolom), 08-arsitektur §4/§12, 00-akun-uji (CNV/WST tanpa aturan demo).
